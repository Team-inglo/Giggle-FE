export interface UserInfoState {
  여권번호: string;
  이름: string;
  성별: string;
  생년월일: string;
  유효기간: string;
  국적: string;
}

export interface userRegistrationCardState {
  등록번호: string;
  체류자격: string;
  발급일자: string;
  테스트: string;
}

export interface employInfo {
  전화번호 : string;
  이메일 : string;
}