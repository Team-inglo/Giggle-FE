export interface UserInfoState {
  여권번호: string;
  이름: string;
  체류자격: string;
  체류기간: string;
  발급일: string;
  국적: string;
}

export interface userRegistrationCardState {
  등록번호: string;
  이름: string;
  국적: string;
  체류자격: string;
  발급일자: string;
  발급번호: string;
}